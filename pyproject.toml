[project]
name = "fastshell"
version = "0.1.0"
description = "mcp for ruunning shell commands in FastAPI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "asyncio>=3.4.3",
    "mcp>=1.9.4",
    "websockets>=15.0.1",
]
